#include <stdio.h>

int main() {
    int x1, y1, x2, y2;
    float x, y;
    scanf("%d%d%d%d", &x1,&y1,&x2,&y2);
    x = (x1 + x2) / 2.0;
    y = (y1 + y2) / 2.0;
    printf("Binoy's house is located at (%.1f,%.1f)\n",x,y);

    return 0;
}
