#include <math.h>
#include <stdio.h>

int main() {
    float t, v, WCF;
    scanf("%f %f", &t, &v);
    WCF = 35.74 + (0.6215 * t) - (35.75 * pow(v, 0.16)) + (0.4275 * t * pow(v, 0.16));
    printf("%.2f\n", WCF);

    return 0;
}
